export interface products {
  id: number;
  name: string;
  category: string;
  price: number;
  description: string;
  image: string;
}

export interface categories {
  id: number;
  name: string;
  image: string;
}
